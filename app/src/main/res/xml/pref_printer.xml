<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

<!--    <SwitchPreference
        android:defaultValue="true"
        android:key="example_switch"
        android:summary="@string/pref_description_social_recommendations"
        android:title="@string/pref_title_social_recommendations" />

    &lt;!&ndash; NOTE: EditTextPreference accepts EditText attributes. &ndash;&gt;
    &lt;!&ndash; NOTE: EditTextPreference's summary should be set to its value by the activity code. &ndash;&gt;
    <EditTextPreference
        android:capitalize="words"
        android:defaultValue="@string/pref_default_display_name"
        android:inputType="textCapWords"
        android:key="example_text"
        android:maxLines="1"
        android:selectAllOnFocus="true"
        android:singleLine="true"
        android:title="@string/pref_title_display_name" />-->

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->

    <ListPreference
        android:defaultValue="2"
        android:entries="@array/pref_lists_printers"
        android:entryValues="@array/pref_lists_printers_values"
        android:key="printer_list"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_printers"
        android:summary="Select default printer model for print outs."/>

    <SwitchPreference
        android:defaultValue="false"
        android:key="switch_printer_enabled"
        android:title="Enable or disable printing"
        android:summary="This will enable or disable the printing function when performing transactions."/>

</PreferenceScreen>
